<apex:page controller="DataUtillityClass" lightningStylesheets="true">
    <apex:form >
        <apex:pageBlock title="Accounts">
            <apex:dataList value="{!Accounts}" var="rec">
                {!rec.Name}
                {!rec.Id}
                
          </apex:dataList>
        </apex:pageBlock>

<apex:pageBlock title="contactsdata">
            <apex:dataList value="{!contactsdata}" var="rec">
                {!rec.Name}
                {!rec.Id}
                
            </apex:dataList>
        </apex:pageBlock>
    </apex:form>
</apex:page>